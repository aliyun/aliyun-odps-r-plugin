name: Building RODPS

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false

    steps:
    - uses: actions/checkout@v3
    - uses: r-lib/actions/setup-r@v2
    - name: Install deps
      run: |
        R --no-save -e "install.packages('devtools', repo='https://mirrors.nju.edu.cn/CRAN')"
    - name: Building package
      run: |
        ./tools/package.sh
